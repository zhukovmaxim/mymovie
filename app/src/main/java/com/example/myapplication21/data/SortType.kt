package com.example.myapplication21.data

enum class SortType {
    DATE_DESC,      // По дате добавления (новые сначала)
    DATE_ASC,       // По дате добавления (старые сначала)
    RATING_DESC,    // По оценке (высокие сначала)
    RATING_ASC,     // По оценке (низкие сначала)
    YEAR_DESC,      // По году выпуска (новые сначала)
    YEAR_ASC,       // По году выпуска (старые сначала)
    TITLE_ASC,      // По алфавиту (А-Я)
    TITLE_DESC      // По алфавиту (Я-А)
}


